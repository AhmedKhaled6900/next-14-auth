/** @type {import('next').NextConfig} */
const nextConfig = {


}

module.exports = {
    ...nextConfig ,
    // reactStrictMode: false,
    images: {
        domains: ["res.cloudinary.com"],
    }
}
